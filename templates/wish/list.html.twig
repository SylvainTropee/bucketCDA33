{% extends 'base.html.twig' %}

{% block body %}
    <h2>Liste de souhaits !</h2>


    {% for wish in wishes %}

        <div>
            <a href="{{ path('wish_detail', {'id': wish.id}) }}">{{ wish.title }}</a>
        </div>

    {% endfor %}


{% endblock %}

{% block title %}

{% endblock %}

